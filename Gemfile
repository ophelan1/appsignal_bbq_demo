source "https://rubygems.org"

ruby ">= 3.2.0"

# Rails 8.1 is the current actively-supported series (8.1.3.1, July 2026).
gem "rails", "~> 8.1.0"

gem "sqlite3", ">= 2.1"
gem "puma", ">= 6.0"

# The point of this app: real APM data in AppSignal.
gem "appsignal"

group :development, :test do
  gem "debug", require: false
end
